
const users = [
    {firstName : "Frederick", lastName : "Adomey", email : "frederick@gmail.com", password : "fred123"},
    {firstName : "Francis", lastName : "Adomey", email : "francis@gmail.com", password : "fran123"}
]

const first_name = document.getElementById("first_name").value
const last_name = document.getElementById("last_name").value
const input_email = document.getElementById("email").value
const input_password = document.getElementById("password").value

for (let index = 2; index < array.length; index++) {
     index = users[index];
    
    function addToArray(index) {
        if (true) {
            users[index].firstName.push("first_name")
            users[index].lastName.push("last_name")
            users[index].email.push("input_email")
            users[index].password.push('input_password')
        }
    }
    addToArray(index)
}
addToArray(index)
